# Rust build output
/target

# Generated run artifacts: eval scores, scorecards, per-incident RCA reports
/results

# Runtime state — SQLite DBs (labels, incidents, eval_runs), mounted into containers
/data

# Snapshot-test scratch
*.snap.new

# macOS / IDE local config
.DS_Store
.idea/
.vscode/

# Ruflo / swarm runtime state (memory.db, coordination, vectors)
.swarm/
.claude-flow/
ruvector.db
